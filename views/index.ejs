<!DOCTYPE html>
<html lang="en">

<head>
    <title>Parish Church html Template</title>
    <meta charset="utf-8">
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Calling Favicon -->
    <link rel="shortcut icon" type="image/x-icon" href="/images/favicon.png">
    <!-- Calling Favicon -->
    <!-- ********************* -->
    <!-- CSS files -->
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/animate.css">
    <link rel="stylesheet" href="/css/all.min.css">
    <link rel="stylesheet" href="/css/owl.carousel.min.css">
    <link rel="stylesheet" href="/css/nice-select.css">
    <link rel="stylesheet" href="/css/magnific-popup.css">
    <link rel="stylesheet" href="/css/meanmenu.css" media="all">
    <link rel="stylesheet" href="/css/default.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" class="color-changing" href="/css/color6.css">
    <link rel="stylesheet" href="/css/responsive.css">
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.14.7/dist/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
    <!-- End CSS files -->

    <style>
  /* Set fixed width and height for the testimony images */
  .album-gl img {
    width: 200px; /* Adjust this to the size you want */
    height: 200px; /* Adjust this to the size you want */
    object-fit: cover; /* Ensures the image keeps its aspect ratio and fills the container */
  }

  /* Ellipsis for long messages */
  .testimony-message {
    display: -webkit-box;
    -webkit-line-clamp: 3; /* Limit the message to 3 lines */
    -webkit-box-orient: vertical;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* Ensure pagination stays on the next line and is centered */
  .pagination-wrapper {
    display: flex;
    justify-content: center; /* Centers the pagination horizontally */
    width: 100%;
    margin-top: 30px; /* Add space between content and pagination */
  }
</style>
</head>


       <div class="modal fade" id="signupModal" tabindex="-1" role="dialog" aria-labelledby="signupModalTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                    
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <h2 class="f-700 mb-12">Create an account to get notified when a testimony is posted </h2>
                        <!-- <p>Quisque enim ipsum, commodo et venenatis rutrum, luctus in enim venenatis.</p> -->
                    </button>
                    </div>
                    <div class="modal-body">
                      <form action="#" id="signupForm">
                        <div class="form-group relative mb-25 mb-sm-20">
                          <input type="text" class="form-control input-lg input-white shadow-5" id="name" placeholder="Name">
                          <i class="far fa-user transform-v-center"></i>
                          <span id="nameError" class="text-danger"></span> <!-- Error span -->
                        </div>
                        <div class="form-group relative mb-25 mb-sm-20">
                          <input type="email" class="form-control input-lg input-white shadow-5" id="signup-email" placeholder="Email">
                          <i class="far fa-envelope transform-v-center"></i>
                          <span id="signupEmailError" class="text-danger"></span> <!-- Error span -->
                        </div>
                        <div class="form-group relative mb-20 mb-sm-20">
                          <input type="password" class="form-control input-lg input-white shadow-5" id="pwd" placeholder="Password">
                          <i class="fas fa-lock transform-v-center"></i>
                          <span id="pwdError" class="text-danger"></span> <!-- Error span -->
                        </div>
                        <div class="form-group relative mb-20 mb-sm-20">
                          <input type="password" class="form-control input-lg input-white shadow-5" id="pwd-confirm" placeholder="Confirm Password">
                          <i class="fas fa-lock transform-v-center"></i>
                          <span id="pwdConfirmError" class="text-danger"></span> <!-- Error span -->
                        </div>
                        <div class="form-group form-check pl-0">
                          <div class="d-flex justify-content-between">
                            <div class="custom-control custom-checkbox">
                                <input type="checkbox" class="custom-control-input" id="customCheck1" checked="">
                                <label class="custom-control-label fs-13" for="customCheck1"><span class="label-check">Remember me</span></label>
                            </div>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-black btn-block shadow-4 mt-20">SIGNUP</button>
                        <p class="text-center mt-3">
                          Already have an account? 
                          <a href="#signinModal" data-toggle="modal" data-dismiss="modal" class="text-primary">
                              Login
                          </a>
                        </p>
                      </form>
                    </div>
                
                </div>
        </div>
    </div>

    <div class="modal fade" id="signinModal" tabindex="-1" role="dialog" aria-labelledby="signinModalTitle" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                    
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <h2 class="f-700 mb-12">Sign in to your account to get notified when a testimony is posted</h2>
                        <!-- <p>Quisque enim ipsum, commodo et venenatis rutrum, luctus in enim venenatis.</p> -->
                    </button>
                    </div>
                    <div class="modal-body">
                      <form action="#" id="loginForm">
                        <div class="form-group relative mb-25">
                          <input type="email" class="form-control input-lg input-white shadow-5" id="email2" placeholder="Email">
                          <i class="far fa-envelope transform-v-center"></i>
                          <span id="emailError" class="text-danger"></span> <!-- Error span -->
                        </div>
                        <div class="form-group relative mb-25">
                          <input type="password" class="form-control input-lg input-white shadow-5" id="pwd2" placeholder="Password">
                          <i class="fas fa-lock transform-v-center"></i>
                          <span id="loginPwdError" class="text-danger"></span> <!-- Error span -->
                        </div>
                        <div class="form-group form-check pl-0">
                          <div class="d-flex justify-content-between">
                            <div class="custom-control custom-checkbox">
                                <input type="checkbox" class="custom-control-input" id="customCheck1" checked="">
                                <label class="custom-control-label fs-13" for="customCheck1"><span class="label-check">Remember me</span></label>
                            </div>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-black btn-block shadow-4 mt-20">LOGIN</button>
                        <p class="text-center mt-3">
                          Don't have an account? 
                          <a href="#signupModal" data-toggle="modal" data-dismiss="modal" class="text-primary">
                              Sign Up
                          </a>
                        </p>
                      </form>
                    </div>
                </div>
            </div>
        </div>

<body>
    <!-- Preloader start -->
    <div class="loader-out flex-center">
        <div class="loader"></div>
    </div>
    <!-- Preloader end -->
    <!-- Header start -->
    
  
    <!-- The search Modal start -->
    <%- include('header') %>

    <div class="d-flex justify-content-center align-items-center my-4">
        <p class="text-center mb-0" style="font-size: 18px; font-weight: bold;">
            Do you want to share what the Lord has done for you?? 
            <a href="/create-testimony" class="btn btn-primary btn-sm ml-2">
                Click Here
            </a>
        </p>
    </div>


    <!--Inner end -->
    <!-- Album gallery start -->
    <section class="albums pt-80 pb-80">
        <div class="container">
            <div class="row mb-4">
                <div class="col-12 text-center">
                    <select id="categoryFilter" class="form-control w-auto d-inline">
                        <option value="all">All Categories</option>
                        <% categories.forEach((category) => { %>
                            <option value="<%= category.uuid %>"><%= category.name %></option>
                        <% }) %>
                    </select>
                </div>
            </div>
            <div class="row align-items-center">
              <% if (testimonies.length > 0) { %>
                    <% testimonies.forEach((testimony) => { %>
                        <div class="col-lg-4 col-md-6 text-center testimony-item" data-category="<%= testimony.category?.uuid %>">
                            <div class="album-gl album-gl2">
                                <img src="<%= testimony.image %>" alt="Testimony Image">
                                <h3 class="fs-25 f-700 mb-10 mt-20">
                                    <%= testimony.firstname %> <%= testimony.lastname %>
                                    <% if (testimony.isFeatured) { %> 
                                        <p class="badge badge-success ml-2">Featured</p>
                                    <% } %>
                                </h3>
                                <p class="mb-15 testimony-message">
                                    <%= testimony.testimony.replace(/<\/?[^>]+(>|$)/g, '') %>
                                </p>
                                <a href="/testimony/<%= testimony.uuid %>" class="f-700 yellow fs-14 uppercase">
                                    Read More
                                    <i class="fas fa-long-arrow-alt-right ml-10"></i>
                                </a>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="col-12 text-center">
                        <h1 class="fs-35 f-800">No testimonies at the moment</h1>
                    </div>
                <% } %>
            </div>

            <!-- Pagination section -->
            <div class="pagination-wrapper">
                <div class="pagination-sml text-center" style="padding-top: 30px">
                    <ul>
                        <!-- Previous page link -->
                        <% if (pagination.currentPage > 1) { %>
                            <li>
                                <a href="?page=<%= pagination.currentPage - 1 %>">
                                    <i class="fas fa-angle-double-left"></i>
                                </a>
                            </li>
                        <% } %>

                        <!-- Pagination numbers -->
                        <% for (let i = 1; i <= pagination.totalPages; i++) { %>
                            <li class="<%= pagination.currentPage === i ? 'active' : '' %>">
                                <a href="?page=<%= i %>"><%= i %></a>
                            </li>
                        <% } %>

                        <!-- Next page link -->
                        <% if (pagination.currentPage < pagination.totalPages) { %>
                            <li>
                                <a href="?page=<%= pagination.currentPage + 1 %>">
                                    <i class="fas fa-angle-double-right"></i>
                                </a>
                            </li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </div>
    </section>

    <!-- Album gallery end -->
    <!-- cta start -->
    <section class="cta bg-yellow pt-30 pb-30">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-9 col-md-12 text-center text-lg-left">
                    <h3 class="f-700 fs-35 mb-md-20">Not A Member? Connect With Us... </h3>
                </div>
                <div class="col-lg-3 col-md-12 text-center text-lg-right"> <a href="contact-1.html" class="btn btn-black shadow-1">CONNECT WITH US</a>
                </div>
            </div>
        </div>
    </section>
    <!-- cta end -->
    <!-- footer start -->
    <%- include('footer') %>


<script src="../../marketing-utilities//buy-button.js" data-template-name="parish"></script>

    <!-- footer end -->
    <!-- ********************* -->
    <!-- Theme skins -->
    <div class="color-theme transition-4">
        <div class="theme-switch">
            <span class="switch">
           <i class="fa fa-eye"></i>
         </span>
        </div>
        <div class="theme-colors transition-4">
            <h4>Theme Skins:</h4>
            <ul class="theme-ul">
                <li>
                    <span class="each-color co6" data-color="/css/color6.css">
                   <img src="/images/co-6.png" alt="">
               </span>
                </li>
                <li>
                    <span class="each-color co3" data-color="/css/color.css">
                   <img src="/images/co-1.png" alt="">
               </span>
                </li>
                <li>
                    <span class="each-color co2" data-color="/css/color2.css">
                   <img src="/images/co-2.png" alt="">
               </span>
                </li>
                <li>
                    <span class="each-color co1" data-color="/css/color3.css">
                     <img src="/images/co-3.png" alt="">
                 </span>
                </li>
                <li>
                    <span class="each-color co4" data-color="/css/color4.css">
                     <img src="/images/co-4.png" alt="">
                 </span>
                </li>
                <li>
                    <span class="each-color co5" data-color="/css/color5.css">
                     <img src="/images/co-5.png" alt="">
                 </span>
                </li>
                <li>
                    <span class="each-color co7" data-color="/css/color7.css">
                     <img src="/images/co-7.png" alt="">
                 </span>
                </li>
                <li>
                    <span class="each-color co8" data-color="/css/color8.css">
                     <img src="/images/co-8.png" alt="">
                 </span>
                </li>
                <li>
                    <span class="each-color co9" data-color="/css/color9.css">
                     <img src="/images/co-9.png" alt="">
                 </span>
                </li>
            </ul>
            <p class="fs-13 "><b>Note: </b>This will not change any image colors.</p>
        </div>
    </div>
    <!-- Theme skins -->
    <!-- JS Files -->
    <script src="/js/modernizr-3.5.0.min.js"></script>
    <script src="/js/jquery-1.12.4.min.js"></script>
    <script src="/js/bootstrap.bundle.min.js"></script>
    <script src="/js/owl.carousel.min.js"></script>
    <script src="/js/jquery.magnific-popup.min.js"></script>
    <script src="/js/jquery.nice-select.min.js"></script>
    <script src="/js/jquery.waypoints.min.js"></script>
    <script src="/js/jquery.counterup.min.js"></script>
    <script src="/js/jquery.countdown.min.js"></script>
    <script src="/js/lightslider.min.js"></script>
    <script src="/js/wow.min.js"></script>
    <script src="/js/isotope.pkgd.min.js"></script>
    <script src="/js/jquery.meanmenu.min.js"></script>
    <script src="/js/tilt.jquery.min.js"></script>
    <script src="/js/main.js"></script>
    <script src="/js/audio-file.js"></script>
    <!-- <script src="/js/modal.js"></script> -->
    <!-- JS Files end -->

    <script>
  document.addEventListener("DOMContentLoaded", function() {
    // Modal element
    const modal = document.getElementById('signupModal');

    // Check if user is logged in by checking a variable set by the backend
    const isLoggedIn = <%= loggedIn ? 'true' : 'false' %>; // Comes from backend, defined later
    if (isLoggedIn) {
      // If the user is logged in, don't show the modal
      $(modal).modal('hide');
    } else {
      // If the user is not logged in, show the modal
      $(modal).modal('show');
    }

      function isValidEmail(email) {
        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        return emailRegex.test(email);
      }

      // Sign Up Form Validation and Submission
      document.getElementById('signupForm')
        .addEventListener('submit', async function (event) {
          event.preventDefault();
          
          // Clear previous error messages
          document.getElementById('nameError').innerText = '';
          document.getElementById('signupEmailError').innerText = '';
          document.getElementById('pwdError').innerText = '';
          document.getElementById('pwdConfirmError').innerText = '';

          const name = document.getElementById('name').value.trim();
          const email = document.getElementById('signup-email').value.trim();
          const password = document.getElementById('pwd').value.trim();
          const confirmPassword = document.getElementById('pwd-confirm').value.trim();

          let isValid = true;

          // Validation checks
          if (!name) {
            document.getElementById('nameError').innerText = 'Name is required';
            isValid = false;
          }
          if (!email) {
            document.getElementById('signupEmailError').innerText = 'Email is required';
            isValid = false;
          } else if (!/\S+@\S+\.\S+/.test(email)) {
            document.getElementById('signupEmailError').innerText = 'Invalid email address';
            isValid = false;
          }
          if (!password) {
            document.getElementById('pwdError').innerText = 'Password is required';
            isValid = false;
          }
          if (password !== confirmPassword) {
            document.getElementById('pwdConfirmError').innerText = 'Passwords do not match';
            isValid = false;
          }

          if (!isValid) return;

          // Proceed with signup
          const signupResponse = await fetch(`/users`, {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify({ fullname: name, email, password })
          });

          if (signupResponse.ok) {
            window.location.reload();
          } else {
            const error = await signupResponse.json();
            alert('Error: ' + error.message);
          }
        });

      // Login Form Validation and Submission
      document.getElementById('loginForm')
        .addEventListener('submit', async function (event) {
          event.preventDefault();

          // Clear previous error messages
          document.getElementById('emailError').innerText = '';
          document.getElementById('loginPwdError').innerText = '';

          const email = document.getElementById('email2').value.trim();
          const password = document.getElementById('pwd2').value.trim();

          let isValid = true;

          // Validation checks
          if (!email) {
            document.getElementById('emailError').innerText = 'Email is required';
            isValid = false;
          } else if (!/\S+@\S+\.\S+/.test(email)) {
            document.getElementById('emailError').innerText = 'Invalid email address';
            isValid = false;
          }
          if (!password) {
            document.getElementById('loginPwdError').innerText = 'Password is required';
            isValid = false;
          }

          if (!isValid) return;

          // Proceed with login
          const loginResponse = await fetch(`/auth/login`, {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify({ email, password })
          });

          if (loginResponse.ok) {
            window.location.reload();
          } else {
            const error = await loginResponse.json();
            alert('Error: ' + error.message);
          }
        });

  });
</script>
<script>
  document.getElementById('categoryFilter').addEventListener('change', (event) => {
    const selectedCategory = event.target.value;
    console.log("selected", selectedCategory);
    const testimonies = document.querySelectorAll('.testimony-item');
    console.log("testimonies", testimonies); 

    testimonies.forEach((testimony) => {
      const testimonyCategory = testimony.getAttribute('data-category');
      console.log("testimonyCategory", testimonyCategory);
      if (selectedCategory === 'all' || testimonyCategory === selectedCategory) {
        testimony.style.display = 'block';
      } else {
        testimony.style.display = 'none'
      }
    });
  });
</script>

</body>

</html>
